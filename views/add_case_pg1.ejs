<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content="" />
    <meta name="author" content="" />

    <title>Register</title>

    <!-- Custom fonts for this template-->
    <link
      href="../static/vendor/fontawesome-free/css/all.min.css"
      rel="stylesheet"
      type="text/css"
    />
    <link
      href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
      rel="stylesheet"
    />

    <!-- Custom styles for this template-->
    <link href="../static/css/sb-admin-2.min.css" rel="stylesheet" />
  </head>
  <body class="bg-gradient-primary">
    <div class="container">
      <div class="card o-hidden border-0 shadow-lg my-5">
        <div class="card-body p-0">
          <div class="p-5">
            <!-- <a id="addcase" href="/client/dashboard" method="GET"
              ><span>back</span></a
            > -->
            <div class="text-center">
              <h1 class="h4 text-gray-900 mb-4">
                Please fill up the following details to add a case
              </h1>
            </div>
            <form id="addcase" action="/client/add_case_pg1" method="POST">
              <div class="form-group row mt-4">
                <label for="case_name">Case Name:</label>
                <input
                  placeholder="Give the case a short name"
                  class="form-control form-control-user"
                  name="case_name"
                  type="text"
                  id="case_name"
                  required
                  autofocus
                />
              </div>
              <div class="row mt-4">
                <label for="case_type">Select the type of case</label>
                <select
                  type="case_type"
                  name="case_type"
                  id="case_type"
                  class="form-control form-control-user"
                >
                  <option value="Criminal">Criminal</option>
                  <option value="Civil">Civil</option>
                  <option value="Family matters">Family matters</option>
                  <option value="Land">Land</option>
                  <option value="Co op court dispute">
                    Co op court dispute
                  </option>
                  <option value="Charity trust">Charity trust</option>
                  <option value="SEBI">SEBI</option>
                </select>
              </div>
              <div class="row mt-4">
                <label for="case_descp">Case description</label>
                <textarea
                  placeholder="Enter the case details here...."
                  name="case_descp"
                  class="form-control form-control-user"
                  id="case_descp"
                  required
                ></textarea>
              </div>
              <div class="row mt-4">
                <input
                  name="submit"
                  type="submit"
                  value="Proceed"
                  id="addcase-submit"
                  class="btn btn-primary btn-user btn-block"
                />
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
